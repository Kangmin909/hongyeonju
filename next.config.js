/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Ensure we can import from outside if needed, though usually not recommended in Next.js structure
};

module.exports = nextConfig;
